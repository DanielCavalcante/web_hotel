<%= form_for @room, :html => {:class => "form-horizontal"} do |f| %>
  <% if @room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@room.errors.count, "error") %> prohibited this room from being saved:</h2>

      <ul>
      <% @room.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :number, class: 'col-md-3 control-label' %>
    <div class="col-md-6">
      <%= f.number_field :number, class: 'form-control' %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :floor, class: 'col-md-3 control-label' %>
    <div class="col-md-6">
      <%= f.number_field :floor, class: 'form-control' %>
    </div>
  </div>
  <%= f.submit t('actions.save'), class: 'btn btn-success col-md-3 col-md-offset-3' %>
  <%= link_to t('actions.back'), rooms_path, class: 'btn btn-default col-md-3' %>
<% end %>
